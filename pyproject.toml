[project]
name = "local-logger-converter"
version = "0.1.0"
description = "Convert local-logger JSONL files to Parquet format"
requires-python = ">=3.10"
dependencies = [
    "polars>=0.20.0",
    "pyarrow>=14.0.0",
]
